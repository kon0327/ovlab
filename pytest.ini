[pytest]
markers =
    libero: requires the pinned LIBERO runtime and local assets
    openvla: requires the tested local OpenVLA runtime and checkpoint
    gpu: may require EGL GPU rendering
    manual: runs only after explicit opt-in
