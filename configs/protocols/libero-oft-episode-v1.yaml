schema_version: "0.1.0"
kind: protocol
extends: libero-oft-smoke-v1.yaml
id: libero-oft-episode-v1

execution:
  # One complete LIBERO episode. The environment may still terminate earlier
  # on genuine task success or a benchmark terminal signal.
  maximum_episode_steps: 520
