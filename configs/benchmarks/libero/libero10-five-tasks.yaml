schema_version: "0.1.0"
kind: benchmark
type: libero
extends: libero10-smoke.yaml

settings:
  # Five distinct LIBERO-10 scenarios. A protocol with one rollout per task
  # produces exactly five episodes.
  task_indices: [0, 1, 2, 3, 4]
