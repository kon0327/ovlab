schema_version: "0.1.0"
kind: experiment

deployment:
  profile: openvla
  renderer: egl

experiment:
  id: libero10-openvla-lora-merged-rpc-smoke
  name: Merged OpenVLA-LoRA methodological reference on LIBERO-10 through local RPC
  tags: [libero-10, lora, merged-full-weights, rpc, smoke, methodological-reference]

components:
  benchmark: benchmarks/libero/libero10-smoke.yaml
  policy: policies/openvla-lora/merged-libero10.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-smoke-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
