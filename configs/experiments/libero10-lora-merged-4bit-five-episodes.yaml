schema_version: "0.1.0"
kind: experiment

deployment:
  profile: openvla
  renderer: egl

experiment:
  id: libero10-openvla-lora-merged-4bit-five-episodes
  name: Quantized merged OpenVLA-LoRA methodological five-episodes on LIBERO-10
  tags: [libero-10, lora, merged-full-weights, 4bit, nf4, rpc, five-episodes]

components:
  benchmark: benchmarks/libero/libero10-five-tasks.yaml
  policy: policies/openvla-lora/merged-libero10-4bit.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-official-episode-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
