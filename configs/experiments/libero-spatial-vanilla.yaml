schema_version: "0.1.0"
kind: experiment

experiment:
  id: libero-spatial-openvla-vanilla
  name: OpenVLA Vanilla on LIBERO-Spatial
  tags: [libero, spatial, vanilla]

components:
  benchmark: benchmarks/libero/spatial.yaml
  policy: policies/openvla-vanilla/base.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-standard-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
