schema_version: "0.1.0"
kind: experiment

deployment:
  profile: openvla
  renderer: egl

experiment:
  id: libero10-lora-full-three-per-task
  name: Unquantized merged OpenVLA-LoRA on all LIBERO-10 tasks, three episodes each
  tags: [libero-10, lora, merged-full-weights, unquantized, bf16, rpc, 30-episodes]

components:
  benchmark: benchmarks/libero/libero10.yaml
  policy: policies/openvla-lora/merged-libero10.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-three-episodes-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
