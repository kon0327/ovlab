schema_version: "0.1.0"
kind: experiment

deployment:
  profile: oft
  renderer: egl

experiment:
  id: libero10-oft-4bit-three-per-task
  name: 4-bit OpenVLA-OFT on all LIBERO-10 tasks, three episodes each
  tags: [libero-10, openvla-oft, 4bit, nf4, action-chunk, rpc, 30-episodes]

components:
  benchmark: benchmarks/libero/libero10-oft.yaml
  policy: policies/openvla-oft/libero10-native-4bit.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-oft-three-episodes-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
