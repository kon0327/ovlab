schema_version: "0.1.0"
kind: experiment

deployment:
  profile: openvla
  renderer: egl

experiment:
  id: libero10-openvla-lora-merged-4bit-dynamic-bundle-smoke
  name: Dynamic config bundle smoke for quantized merged OpenVLA-LoRA on LIBERO-10
  tags: [libero-10, lora, merged-full-weights, 4bit, nf4, rpc, smoke, dynamic-config-bundle]

components:
  benchmark: benchmarks/libero/libero10-smoke.yaml
  policy: policies/openvla-lora/merged-libero10-4bit.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-smoke-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
