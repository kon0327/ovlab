schema_version: "0.1.0"
kind: experiment

experiment:
  id: libero10-openvla-vanilla-rpc-smoke
  name: Full-weight suite-finetuned OpenVLA reference on LIBERO-10 through local RPC
  tags: [libero-10, vanilla, full-weight, rpc, smoke, integration-reference]

components:
  benchmark: benchmarks/libero/libero10-smoke.yaml
  policy: policies/openvla-vanilla/base.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-smoke-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml

