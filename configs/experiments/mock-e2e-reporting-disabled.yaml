schema_version: "0.1.0"
kind: experiment

reporting:
  enabled: false
  profile: libero-task-default
  on_task_finalize: false
  on_run_finalize: false
  failure_policy: warn

experiment:
  id: mock-e2e-smoke
  name: Dependency-light mock end-to-end smoke
  tags: [mock, smoke, runner]

components:
  benchmark: benchmarks/mock/base.yaml
  policy: policies/mock/base.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/smoke-v1.yaml
  action_interface: interfaces/actions/mock-delta-gripper-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
