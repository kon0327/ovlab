schema_version: "0.1.0"
kind: experiment

deployment:
  profile: openvla
  renderer: egl

experiment:
  id: libero10-openvla-vanilla-4bit-rpc-smoke
  name: Cross-suite OpenVLA Vanilla NF4 diagnostic on LIBERO-10 through local RPC
  tags: [libero-10, vanilla, base-model, 4bit, nf4, rpc, smoke, cross-suite]

components:
  benchmark: benchmarks/libero/libero10-smoke.yaml
  policy: policies/openvla-vanilla/4bit.yaml
  metrics: metrics/action-safe-v1.yaml
  protocol: protocols/libero-smoke-v1.yaml
  action_interface: interfaces/actions/libero-osc-pose-v1.yaml
  artifacts: artifacts/filesystem.yaml

resources:
  registry: resources/registry.yaml
