[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ovlab-policy-sdk"
version = "0.1.0"
description = "Abstract policy adapter boundary for OpenVLABenchmark"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["ovlab-core>=0.1.0"]

[tool.setuptools.packages.find]
where = ["src"]
