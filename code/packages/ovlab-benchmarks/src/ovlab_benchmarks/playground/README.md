# Playground benchmark

Playground is reserved for a future interactive environment in which users can issue or revise robot instructions during execution. Its interface will represent instructions as timestamped real-time events instead of assuming one immutable instruction per episode.
